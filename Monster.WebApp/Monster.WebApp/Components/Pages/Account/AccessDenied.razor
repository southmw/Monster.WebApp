@page "/account/access-denied"
@inject NavigationManager NavigationManager

<PageTitle>접근 거부 - Monster.WebApp</PageTitle>

<MudContainer MaxWidth="MaxWidth.Small" Class="mt-8">
    <MudPaper Elevation="3" Class="auth-container">
        <div class="auth-header">
            <MudIcon Icon="@Icons.Material.Filled.Block" Size="Size.Large" Color="Color.Error" />
            <MudText Typo="Typo.h4" Style="font-weight: 700; margin-top: 16px;">접근 권한이 없습니다</MudText>
            <MudText Typo="Typo.body2" Color="Color.Secondary" Style="margin-top: 8px;">
                이 페이지에 접근할 수 있는 권한이 없습니다.
            </MudText>
        </div>

        <MudDivider Class="my-4" />

        <MudText Typo="Typo.body1" Align="Align.Center" Class="mb-4" Color="Color.Secondary">
            필요한 권한이 없거나 로그인이 필요합니다.
            <br />
            문제가 계속되면 관리자에게 문의하세요.
        </MudText>

        <MudStack Row="true" Justify="Justify.Center" Spacing="2" Class="mt-4">
            <MudButton Variant="Variant.Filled"
                       Color="Color.Primary"
                       Href="/"
                       StartIcon="@Icons.Material.Filled.Home">
                홈으로
            </MudButton>
            <MudButton Variant="Variant.Outlined"
                       Color="Color.Default"
                       OnClick="GoBack"
                       StartIcon="@Icons.Material.Filled.ArrowBack">
                이전 페이지
            </MudButton>
        </MudStack>

        <MudDivider Class="my-4" />

        <MudText Typo="Typo.body2" Align="Align.Center" Color="Color.Secondary">
            로그인이 필요하신가요?
            <MudLink Href="/account/login" Color="Color.Primary">로그인</MudLink>
        </MudText>
    </MudPaper>
</MudContainer>

@code {
    private void GoBack()
    {
        NavigationManager.NavigateTo("javascript:history.back()");
    }
}
